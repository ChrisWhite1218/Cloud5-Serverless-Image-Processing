[project]
name = "ai-lambda"
version = "0.1.0"
description = "Lambda function to generate an AI image from a prompt given in JSON."
requires-python = ">=3.13"
dependencies = [
    "boto3",
    "openai",
]